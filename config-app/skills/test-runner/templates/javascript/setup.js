jest.setTimeout(30000);

beforeAll(() => {
  // {{SETUP_BEFORE_ALL}}
});

afterAll(() => {
  // {{CLEANUP_AFTER_ALL}}
});

beforeEach(() => {
  // {{SETUP_BEFORE_EACH}}
});

afterEach(() => {
  // {{CLEANUP_AFTER_EACH}}
});
