[project]
name = "feishu-bot"
version = "0.1.0"
description = "Feishu Bot with OpenCode Integration via WebSocket"
requires-python = ">=3.10"
dependencies = ["lark-oapi>=1.4.0", "requests>=2.28.0", "sseclient-py>=1.7.2"]

[project.scripts]
feishu-bot = "feishu_bot.main:main"
